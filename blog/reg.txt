当,^不在是在开头的时候,^就变成了一个字符,也就是说[po^nm]+代表的是,po^nm中任意一个字符.而不是po加上nm的取反.
对于捕获组,(first)-(second-(third)), \1代表的是(first),\2代表的是(second-(third)),\3代表的是(third).\0代表的是整个正则表达式.
(a|b)\1 \1代表的是(a|b).但是,在需要进行反向引用的时候,(a|b)的值已经被确定下来了.所以,匹配的结果就是两个相同的字符.